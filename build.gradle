apply plugin: 'java'
apply plugin: 'application'
apply plugin: 'jacoco'

group "org.sample"
version "1.0"

mainClassName = "com.mycompany.app.App"

dependencies {
  testCompile 'junit:junit:4.12'
}

repositories {
    jcenter()
}